
| Session | Question | Date Asked | Asked By | Answer | Answered On | Answered By | Status | AI Generated | Confidence |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| DAM_Training_Usage_Admin | Why were Careers assets renamed incorrectly during migration? Was the  migration tool responsible for the naming changes? |  | @Mayte Eme |  |  |  | Pending | FALSE | 51 |
| DAM_Training_Usage_Admin | What metadata fields should be mandatory upon upload? |  | @Mayte Eme | AEM Assets lets you decide which fields are mandatory by configuring **metadata schemas** for the folders where assets are uploaded. Common required fields are things like *Title, Description, Usage rights/licence, Expiration date, Brand/Business unit, Asset type*, and sometimes *Language* or key tags. The exact set should come from your governance model, not from the product itself. You configure required fields under **Tools → Assets → Metadata Schemas**, marking fields as mandatory for the schema applied to your upload folders (see *Metadata schemas* and *Metadata best practices for AEM Assets*). |  | Adobe | Pending | FALSE | 24 |
| DAM_Training_Usage_Admin | Should we rely on metadata (not folder location) for identifying asset  languages (EN/ES/FR)? |  | @Mayte Eme | For AEM’s built‑in localization and translation tooling, you should not rely only on metadata. Best practice is to use **language‑root folders in DAM** (for example `/content/dam/<project>/en`, `/fr`, `/es`) as the primary way to distinguish languages, because translation projects and language copies depend on that structure. You can still store a language code in metadata (for search and reporting), but folder‑based language roots are the foundation for automation (see *Prepare assets for translation* and *Translate assets in AEM*). |  | Adobe | Pending | FALSE | 39 |
| DAM_Training_Usage_Admin | Can we adjust how asset filenames  display in the asset picker and in the Sites rail so authors can see more of  the filename (avoid truncation)? | @Mayte Eme |  | Out of the box, the asset picker and Sites left rail show a truncated filename with a fixed column width. Authors can see more detail by switching to **List or Column view** in Assets, or by relying on a more descriptive **Title** field that is visible in the picker. Changing how much of the filename appears in the picker or Sites rail itself would require a **UI customization** (front‑end extension of the console); there is no product setting to simply “expand” the filename display. |  | Adobe |  | TRUE | 75 |
| DAM_Training_Usage_Admin | How do static renditions get automatically chosen by out‑of‑the‑box  components? |  | @Mayte Eme | For Sites using Core Components, images coming from DAM are generally selected by the **Adaptive Image Servlet**, which looks at the requested display size and available renditions. It filters renditions by MIME type and then chooses the smallest rendition that is at least as large as the container, to avoid upscaling (see *Adaptive Image Servlet*). Static renditions themselves are created by **Asset Compute / processing profiles** and are then reused by components at render time. |  | Adobe | Pending | FALSE | 38 |
| DAM_Training_Usage_Admin | How can we prevent components from accidentally pulling old or duplicate  assets? |  | @Mayte Eme | Out of the box, AEM does not automatically block authors from reusing older assets, but you can greatly reduce accidental use of outdated or duplicate assets through governance:  • Use **Review/Approval status and expiration metadata** so only approved, in‑date assets are visible in authoring views or search facets.  • Restrict write access to “archive” folders and remove them from common search paths.  • Use **metadata‑driven permissions** or folder‑level permissions to prevent authors from using work‑in‑progress or deprecated assets.  • Train authors to search/filter by tags, status, and date rather than browsing raw folders. More advanced blocking (for example, hard‑blocking specific assets in components) would require project‑specific customization. |  | Adobe | Pending | FALSE | 44 |
| DAM_Training_Usage_Admin | What are the recommended image rendition sizes for SHRSS components  (cards, banners, hero, gallery, tiles, etc.)? |  | @Mayte Eme | Rendition sizes for specific card, banner, hero, gallery, or tile components are **implementation‑specific** and should be derived from your design system and breakpoint definitions. AEM does not ship SHRSS‑specific sizes. Best practice is to:  • Identify the pixel widths actually used by your components at each breakpoint.  • Configure **DAM renditions / processing profiles** or **Dynamic Media image presets** that align to those widths.  • Align Image component **allowed widths** with the same set so the Adaptive Image Servlet or Dynamic Media can pick the closest match efficiently (see *Adaptive Image Servlet* and *Dynamic Media URL & Image Presets*). |  | Adobe | Pending | FALSE | 56 |
| DAM_Training_Usage_Admin | When an author uploads a new asset, how do we ensure the correct  rendition is chosen automatically? |  | @Mayte Eme | If you use the Core Image component, AEM automatically chooses an appropriate rendition at request time via the **Adaptive Image Servlet** or **web‑optimized image delivery**; the author just uploads the asset and places it on the page. The servlet looks at the component’s container size and available renditions, then picks the best fit without upscaling (see *Adaptive Image Servlet*). With Dynamic Media, you instead configure **image presets** and component policies so the component requests the right preset automatically. |  | Adobe | Pending | TRUE | 67 |
| DAM_Training_Usage_Admin | Should static renditions be fully disabled once Dynamic Media is  launched? |  | @Mayte Eme | No. Static renditions are still used for thumbnails, previews, and some internal UI uses even when Dynamic Media is enabled, and they are produced automatically by Asset Compute. You generally **do not disable static renditions altogether**. Instead, you minimize custom static renditions and rely on **Dynamic Media virtual renditions and image presets** for delivery to sites and applications (see *View and manage renditions in Experience Manager Assets* and *Overview of Dynamic Media with AEM Assets*). |  | Adobe | Pending | FALSE | 33 |
| DAM_Training_Usage_Admin | What is the plan for transitioning from static renditions → Dynamic Media  presets? |  | @Mayte Eme | There is no one‑click switch; moving from static renditions to Dynamic Media presets is typically a small project:  1) **Enable and configure Dynamic Media Cloud Service** for the relevant environments.  2) Define **image presets and image profiles** that map to your real use cases (hero, card, thumbnail, etc.).  3) Update **component policies** (for example, the Core Image component) to allow using Dynamic Media and to select presets instead of static renditions.  4) Regression‑test key templates/pages and adjust presets as needed.  5) Gradually refactor any custom code that referenced specific static renditions so that it uses Dynamic Media URLs or presets instead (see *Configure Dynamic Media Cloud Service* and *Dynamic Media URL & Image Presets*). |  | Adobe | Pending | FALSE | 47 |
| DAM_Training_Usage_Admin | When Dynamic Media is implemented, will image presets replace all static  renditions? |  | @Mayte Eme | In a Dynamic Media setup, **image presets become the preferred way to deliver images to channels**, but static renditions are still present and used in the DAM UI and as fallbacks. You should plan for components and external consumers to rely primarily on Dynamic Media URLs and presets, while keeping the default static renditions for thumbnails, previews, and non‑Dynamic‑Media use cases (see *Overview of Dynamic Media with AEM Assets* and *Dynamic Media URL & Image Presets*). |  | Adobe | Pending | FALSE | 36 |
| DAM_Training_Usage_Admin | Do we need dedicated language folders in DAM to support future  localization automation? |  | @Mayte Eme | Yes, if you plan to use AEM’s built‑in translation workflows, it is best practice to have **language‑root folders in DAM** such as `/content/dam/<project>/en`, `/fr`, `/es`, etc. Translation projects and language copies rely on that structure to know which assets belong to which locale (see *Prepare assets for translation* and *Translate assets in AEM*). Metadata can complement this, but language folders are the primary mechanism for automation. |  | Adobe | Pending | FALSE | 47 |
| DAM_Training_Usage_Admin | Can metadata alone be used to identify language-specific versions of  assets? |  | @Mayte Eme | You can record language as metadata (for example, a `language` or `dc:language` field) and use it for search or reporting, but AEM’s out‑of‑the‑box translation and localization features expect **language‑specific folders (language roots)** to separate locales. If you rely only on metadata to distinguish languages, you would need custom logic and workflows to drive automation. Best practice is to combine **language‑root folders** for structure with **language metadata** for discoverability (see *Prepare assets for translation* and *Translate assets in AEM*). |  | Adobe | Pending | FALSE | 33 |
