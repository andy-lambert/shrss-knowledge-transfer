| Session | Question | Date Asked | Asked By | Answer | Answered On | Answered By | Status | AI Generated | Confidence |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Locations | Which fields are sourced from  DPLT, which are managed directly in the CMS? Is it just relation to location  ID? |  | @Mayte Eme | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. AEM as a Cloud Service does not provide a generic locations directory out of the box, so the exact behavior is defined in your project’s design, component dialogs, and integration code. Typically, fields that come from a system of record like DPLT should be treated as read-only in AEM, while author-managed fields live only in the AEM content model (for example, in a Location Content Fragment). Which fields are mapped from DPLT, how status changes affect visibility, and how bulk updates are handled are all defined in the SHRSS solution design. |  | Adobe | Pending | FALSE | 30 |
| Locations | If a location’s status is  changed, how does that impact its visibility or behavior on the site (e.g., Draft, Active, Inactive)? |  | @Mayte Eme | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. AEM as a Cloud Service does not provide a generic locations directory out of the box, so the exact behavior is defined in your project’s design, component dialogs, and integration code. Typically, status changes in the system of record (DPLT) are mapped to filters or flags in AEM and then used by components to include/exclude locations, but the exact mappings must be confirmed in your implementation documentation. |  | Adobe | Pending | FALSE | 30 |
| Locations | When a location is added to  DPLT, does it automatically appear in AEM even if some fields are missing? |  | @Mayte Eme | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. AEM as a Cloud Service does not provide a generic locations directory out of the box, so the exact behavior is defined in your project’s design, component dialogs, and integration code. In many integrations, locations appear in AEM as long as required keys (such as an ID) are present, but the minimum required fields and validation rules are defined in the integration and component logic for SHRSS. |  | Adobe | xq | FALSE | 30 |
| Locations | Is the display or availability  of locations in AEM driven by any specific DPLT fields (e.g., location type, status, region)? |  | @Mayte Eme | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. AEM as a Cloud Service does not provide a generic locations directory out of the box, so the exact behavior is defined in your project’s design, component dialogs, and integration code. Generally, fields like location type, status, or region from DPLT can be used to filter or segment locations in AEM, but which fields are used, and how, must be confirmed in the solution design and component requirements. |  | Adobe | Pending | FALSE | 35 |
| Locations | What are the steps to add  locations to another page in AEM? |  | @Mayte Eme | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. AEM as a Cloud Service does not provide a generic locations directory out of the box, so the exact steps (e.g., selecting a Locations component, configuring filters, or manually selecting locations) are determined by the custom components built for SHRSS. Authors should follow the project’s authoring guide for how the Locations components are configured on pages. |  | Adobe | Pending | FALSE | 25 |
| Locations | How can we add locations without  using the accordions? |  | @Mayte Eme | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. Whether you can render locations without accordions depends on the specific components, templates, and variations implemented by the SHRSS project team. If there is no alternative view in the current component set, it would require new component work or configuration changes. |  | Adobe | Pending | FALSE | 25 |
| Locations | How do we add decorations with  the map? |  | @Mayte Eme | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. The ability to add “decorations” (such as overlays, labels, or custom markers) is controlled by how the map/locator component was implemented on SHRSS (e.g., Google Maps customization in the front end). AEM does not provide built-in map decorations; these are defined by the custom component and front-end code. |  | Adobe | Pending | FALSE | 25 |
| Locations | How can we add only the map and  locations, without including accordions? |  | @Mayte Eme | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. Whether a “map only” configuration is available depends on how the Locations/map component was designed (for example, a mode that hides accordions and shows only the map and markers). If that mode does not exist today, it would require component enhancement. |  | Adobe | Pending | FALSE | 25 |
| Locations | Why are images not displayed for  some locations? |  | @Mayte Eme |  |  |  | Pending | FALSE | 20 |
| Locations | How do we add a location not yet  in the DPLT? |  | @Mayte Eme | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. In many implementations, locations come only from the system of record (DPLT), and manual additions in AEM are either not supported or are handled via a separate “manual location” content model. Whether SHRSS supports manual-only locations and how to create them must be confirmed in your solution design and authoring guide. |  | Adobe | Pending | FALSE | 25 |
| Locations | How do we set up criteria in AEM  to display some locations in some places and some locations in different places |  | @Mayte Eme |  |  |  | Pending | TRUE | 60 |
| Locations | Which DPLT field should be used  to replace the “coming soon” flag? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. Any mapping of a “coming soon” flag from DPLT to AEM (or vice versa) is defined in the data-integration contract and the CF/component model. The correct field to use must be taken from the DPLT–AEM mapping specification for SHRSS. |  | Adobe | Pending | FALSE | 45 |
| Locations | What is the correct process for  bulk updating Location Content Fragments from DPLT changes? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. Bulk updates may be driven by automated sync jobs or API calls; AEM does not have a generic “sync from DPLT” feature. The correct process for SHRSS should be documented in the integration design and may involve scheduled jobs, ETL, or external scripts that update CFs based on DPLT changes. |  | Adobe | Pending | FALSE | 35 |
| Locations | Which fields in the Location  Content Fragment are intended for DPLT management vs. author-managed content? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. Typically, integration-owned fields (IDs, codes, status, coordinates, etc.) should be DPLT-managed, while descriptive or marketing content (e.g., intro copy, tags) can be AEM-author-managed. The exact breakdown is defined in the Location CF model and the DPLT-to-AEM field-mapping document for SHRSS. |  | Adobe | Pending | FALSE | 30 |
| Locations | When a new location is added to  DPLT, does the Location Content Fragment automatically generate for all countries? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. Whether CFs are created for each country or only for specific markets depends on how the integration and CF creation logic were implemented (for example, per locale rules). AEM does not automatically create CFs from DPLT; any such auto-creation is custom code defined for SHRSS. |  | Adobe | Pending | FALSE | 30 |
| Locations | What image specifications  (size/dimensions) are recommended for location photography? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. AEM as a Cloud Service does not provide a generic locations directory out of the box, so the exact behavior is defined in your project’s design, component dialogs, and integration code. In general, location imagery is managed as DAM assets: authors upload sufficiently large, high-quality images (for example at least ~1600px on the long edge) and let the front-end or Dynamic Media handle responsive sizing and cropping. Any enforced dimensions, aspect ratios, or focal-point controls for Locations pages are configured in your custom components or Dynamic Media presets. |  | Adobe | Pending | FALSE | 45 |
| Locations | What happens if authors  mistakenly populate hero imagery or text against governance or usage guidelines? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. Typically, governance for hero imagery and copy is handled via authoring guidelines, review/UAT processes, and potentially AEM workflows, rather than hard technical restrictions. Any validation rules or constraints for Locations hero content would need to be built into the custom components or enforced via review processes defined by SHRSS. |  | Adobe | Pending | FALSE | 35 |
| Locations | Is component display logic  restricted by Line of Business, or is it universal? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. Whether components behave differently by line of business (e.g., hotels vs. cafes vs. corporate) depends on how those business rules were encoded in the component logic and configuration. AEM does not inherently restrict components by line of business; any such rules are custom to SHRSS. |  | Adobe | Pending | FALSE | 30 |
| Locations | If authors incorrectly check “Is  Delivery” for a location, what impact does that have on front-end behavior and search? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. Any “Is Delivery” flag’s effect on search results, filters, or UI is defined by the implementation of the Locations search and display components. AEM itself does not interpret that flag; the component or front-end logic does. |  | Adobe | Pending | FALSE | 30 |
| Locations | Are any fields in the Location  Content Fragment calculated or derived instead of being direct mappings from DPLT? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. Whether any fields are derived (for example, combined address lines, display names, or flags) is determined by the data-transformation logic defined in the integration layer or custom code for SHRSS. AEM does not automatically derive such fields without custom implementation. |  | Adobe | Pending | FALSE | 30 |
| Locations | Are there any fields that are  used exclusively for search, filtering, or display logic but are not visible to authors? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. In many implementations, there can be hidden technical fields (e.g., codes, flags) that drive search/filter logic but are not meant for authors to edit. Whether SHRSS has such fields and which they are must be verified in the Location CF model and component implementation. |  | Adobe | Pending | FALSE | 35 |
| Locations | Are there fields that  determine whether a location is publicly discoverable vs. internal/testing only? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. Public vs. internal/test visibility is usually controlled by one or more flags or status codes in the system of record and corresponding logic in AEM components. The specific fields for SHRSS and how they are interpreted must be taken from your integration and requirements documentation. |  | Adobe | Pending | FALSE | 35 |
| Locations | How is the Days/Hours  information synchronized between DPLT and AEM? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. Synchronization of business hours is typically handled by the integration layer (APIs, jobs, ETL) rather than any generic AEM feature. The exact sync frequency, direction, and conflict behavior for SHRSS should be documented in the technical design. |  | Adobe | Pending | FALSE | 35 |
| Locations | Are specific days/hours  ignored by the front end if certain flags are not present (e.g., holiday closures)? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. Logic such as ignoring certain hours or applying holiday closures would be coded in the front-end or service layer that consumes the location data. AEM does not provide built-in holiday/exception logic; it would be custom for SHRSS. |  | Adobe | Pending | FALSE | 35 |
| Locations | Does the Location Content Fragment  support multiple business hours profiles (e.g., regular vs. holiday)? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. Whether multiple business-hours profiles are supported is defined in the Location CF model and the integration contract with DPLT. AEM Content Fragments can model multiple sets of hours if that structure was designed, but the actual model used for SHRSS must be checked in the CF definition. |  | Adobe | Pending | FALSE | 35 |
| Locations | Can authors override DPLT-provided  hours for specific days (e.g., one-off events)? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. In many integrations, overrides are either not allowed (DPLT is the source of truth) or are modeled as separate override fields. Whether SHRSS supports author overrides and how conflicts are resolved is defined in your solution requirements, not by AEM core behavior. |  | Adobe | Pending | FALSE | 35 |
| Locations | If a location is temporarily  closed, how should that be represented in both DPLT and AEM? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. Typically, temporary closures are represented as a status or flag in the system of record and surfaced in AEM as a display treatment (e.g., “temporarily closed” label). The exact field and display treatment for SHRSS must be confirmed in your data and UX requirements. |  | Adobe | Pending | FALSE | 35 |
| Locations | Are there any service level  expectations for how quickly DPLT updates appear on AEM pages? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. SLAs for propagation time (for example, near-real-time vs. batched) are defined in the integration design and operations agreements for SHRSS, not by AEM itself. |  | Adobe | Pending | FALSE | 40 |
| Locations | How does AEM handle situations  where DPLT data is incomplete or malformed (e.g., missing coordinates)? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. Incomplete or malformed data may be filtered out, cause fallback behavior (e.g., no map marker), or surface as errors depending on how the components and integration were coded. AEM does not automatically repair or infer location data. |  | Adobe | Pending | FALSE | 40 |
| Locations | Is there a validation or alerting  mechanism if DPLT data that AEM relies on becomes inconsistent? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. Any monitoring, validation, or alerting for inconsistent DPLT data would be implemented in the integration layer or observability tooling, not as a standard AEM feature. Whether such checks exist for SHRSS must be confirmed in your operational runbooks. |  | Adobe | Pending | FALSE | 40 |
| Locations | Are there any fallback behaviors  if DPLT is unavailable (e.g., cached data, error messaging)? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. Fallback behaviors (such as using cached data, disabling features, or showing maintenance messaging) are defined by the integration and front-end design for SHRSS, not by default AEM behavior. |  | Adobe | Pending | FALSE | 40 |
| Locations | Where should DPLT-related  integration behavior be documented for future reference? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. Integration behavior should be documented in your solution design documents, technical integration specs, and operational runbooks (for example, an AEM solution design or Confluence space), rather than in AEM product documentation. |  | Adobe | Pending | FALSE | 35 |
| Locations | Can authors see any indicators  in AEM that a location has DPLT issues (e.g., sync failures)? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. AEM does not surface DPLT sync issues by default; any indicators or status flags in the authoring UI would have to be implemented as custom dialogs or console views. Whether such indicators exist for SHRSS must be checked in your implementation. |  | Adobe | Pending | FALSE | 35 |
| Locations | Will the Location Content Fragment  and related components be extended in future waves to support more use cases? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. Any plan to extend the Location CF model or components is part of your roadmap and backlog prioritization, not an AEM product capability. This should be discussed and documented as part of future phases with Adobe PS or your implementation team. |  | Adobe | Pending | FALSE | 35 |
| Locations | Are the components flexible  enough to accommodate future changes to DPLT or business rules without major rework? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. Component flexibility and resilience to upstream changes are architectural qualities that depend on how the SHRSS components were designed (e.g., configuration-driven vs. hard-coded). This needs to be assessed by reviewing the implementation rather than AEM product features. |  | Adobe | Pending | FALSE | 40 |
| Locations | Is there a structured way to  collect new requirements/feedback from authors on the Locations implementation? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. AEM does not provide a built-in requirement/feedback collection mechanism; this is part of your governance and operating model (e.g., intake process, JIRA, or a feedback form). |  | Adobe | Pending | FALSE | 40 |
| Locations | Do certain mapping business rules  (e.g., how many locations are displayed, which ones are featured) live only in custom code? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. Mapping rules such as featured locations, maximum counts, or ranking are typically encoded in the component logic or backend services. They are not provided by AEM core features. |  | Adobe | Pending | FALSE | 40 |
| Locations | Can authors view a consolidated  list of all locations and their key attributes within AEM? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. While AEM can list Content Fragments in the repository, any “consolidated locations console” experience is custom. Whether such a list exists for SHRSS depends on your implementation. |  | Adobe | Pending | FALSE | 40 |
| Locations | Are there known limitations or  edge cases that authors should be aware of when working with Locations? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. Known limitations and edge cases (for example, behavior with incomplete data, extreme coordinates, or missing imagery) should be documented in your SHRSS authoring and implementation guides, not in AEM product documentation. |  | Adobe | Pending | FALSE | 40 |
| Locations | Are any additional environments  or testing tools recommended specifically for Locations? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. In general, Locations should follow the same environments and testing approach as the rest of the site (dev, stage, prod, automated tests, etc.). Any special test data or tools for DPLT integration should be documented in your QA and release process. |  | Adobe | Pending | FALSE | 40 |
| Locations | Should location data be  included in any automated validation or regression checks? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. As a best practice, location data and key user journeys (searching, viewing details, booking) should be part of your functional and regression test suite, but the exact checks and tools are defined by your QA strategy, not by AEM. |  | Adobe | Pending | FALSE | 40 |
| Locations | Are any existing AEM workflows  or processes tied to Location Content Fragments (e.g., approvals)? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. AEM can associate workflows with CFs for approvals, but whether any Location-specific workflows exist for SHRSS is defined in your implementation and governance design. |  | Adobe | Pending | FALSE | 40 |
| Locations | Can Location Content Fragments  be versioned, and if so, how should authors interpret version history for locations? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. AEM does support versioning for Content Fragments, but for locations that are fed from DPLT, version history may reflect both manual edits and automated syncs. Authors should follow SHRSS-specific guidance on when, or whether, to roll back versions of Location CFs, since this may conflict with DPLT as the source of truth. |  | Adobe | Pending | FALSE | 45 |
| Locations | Are there any current pain  points with the DPLT integration that we should be aware of? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. Any known integration issues or technical debt should be captured in your project’s issue tracker and discussed with your implementation team; they are not part of AEM product behavior. |  | Adobe | Pending | FALSE | 40 |
| Locations | Can authors manually control  which countries a location appears under, or is that entirely managed by DPLT? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. In many implementations, country assignment is controlled by the system of record to avoid drift, but whether SHRSS allows manual overrides or multi-country mapping is defined in your location data model and component logic. |  | Adobe | Pending | FALSE | 45 |
| Locations | Is the inability to manually  control country-specific display for a location considered a constraint or by design? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. Whether this is a deliberate design choice (to keep DPLT as the single source of truth) or a constraint that should be revisited is a requirements question for SHRSS, not an AEM product limitation. |  | Adobe | Pending | FALSE | 45 |
| Locations | Why are some images not  displaying on the Design issues example? |  |  |  |  |  | Pending | FALSE | 20 |
| Locations | Is there image position/focal  point control available for location images? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. AEM Assets and Dynamic Media do support focal point and smart-cropping capabilities, but whether these are used for SHRSS Locations is determined by your component and Dynamic Media configuration. Any focal-point UI or behavior for Locations pages is custom to the implementation. |  | Adobe | Pending | FALSE | 45 |
| Locations | Should styling inconsistencies  (accordion width, etc.) be treated as defects or enhancements? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. From a governance standpoint, visual and behavioral changes to shared Locations components should go through your normal change-management process: design review, implementation in a lower environment, and stakeholder UAT before promotion to production (potentially using AEM workflows to coordinate approvals). AEM itself does not enforce this process; it is defined by your team’s operating model and defect/enhancement triage practice. |  | Adobe | Pending | FALSE | 45 |
| Locations | Is there a formal review process  before component changes go live on Locations? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. From a governance standpoint, visual and behavioral changes to shared Locations components should go through your normal change-management process: design review, implementation in a lower environment, and stakeholder UAT before promotion to production (potentially using AEM workflows to coordinate approvals). AEM itself does not enforce this process; it is defined by your team’s operating model. |  | Adobe | Pending | FALSE | 45 |
| Locations | Does the Google Map component  allow manual control over data or is it entirely API-driven from DPLT? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. The map/locator component is custom: AEM provides the content and configuration, but the actual map rendering and behavior are handled by the embedded map library (for example, Google Maps) and your front-end code. Capabilities such as decorations, markers, behavior by line of business, or manual overrides are all defined in that custom component rather than standard AEM features. |  | Adobe | Pending | FALSE | 45 |
| Locations | Should locator behavior (hotels,  cafes, corporate) be configured in AEM or in the mapping service? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. The map/locator component is custom: AEM provides the content and configuration, but the actual map rendering and behavior are handled by the embedded map library and your front-end code. Whether behavior by business type is driven by AEM configuration, DPLT data, or map-service settings is defined by the SHRSS implementation, not by AEM product defaults. |  | Adobe | Pending | FALSE | 45 |
| Locations | Is the Google Map component  functionally different on Locations vs. other pages? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. Any differences in how the map behaves on Locations vs. other pages depend on how the components and templates were implemented for SHRSS. AEM does not impose page-specific behavior for map components. |  | Adobe | Pending | FALSE | 40 |
| Locations | Will map functionality be  reviewed separately as part of mapping-specific governance? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. Governance for map functionality (including performance, UX, and data accuracy) should be defined in SHRSS’s operating model and may involve separate mapping stakeholders, but this is not part of standard AEM product governance. |  | Adobe | Pending | FALSE | 40 |
| Locations | Is the Booking Widget configured  via Experience Fragment or another pattern? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. The booking widget/engine is a custom integration: in many implementations, booking URLs or IDs are stored in component dialog fields or in a linked data source (such as a Location Content Fragment), and then passed through to the booking provider. Whether SHRSS uses Experience Fragments or another pattern for the booking widget is defined in your solution design. |  | Adobe | Pending | FALSE | 45 |
| Locations | Is the “Book Now” modal  referenced in the header section a separate component? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. The “Book Now” modal could be implemented as a separate component, part of the header, or part of the booking widget itself, depending on the SHRSS front-end architecture. This is not an AEM product-level behavior. |  | Adobe | Pending | FALSE | 40 |
| Locations | How does the theme override  button styling in the booking widget, and can we standardize it? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. Button styling is controlled by your front-end theme (CSS/Design System) and how the booking widget consumes those styles. Standardizing styling would be addressed as part of your design system and front-end implementation, not by changing AEM product behavior. |  | Adobe | Pending | FALSE | 40 |
| Locations | What is the difference between  “Default” and “Legacy” modes on the booking/locator components? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. Any “Default” vs. “Legacy” mode is a custom configuration concept for SHRSS components and should be documented in your implementation and authoring guides. AEM does not define these modes generically. |  | Adobe | Pending | FALSE | 40 |
| Locations | Where is the booking URL  configured for each location? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. The booking widget/engine is a custom integration: in many implementations, booking URLs or IDs are stored in component dialog fields or in a linked data source (such as a Location Content Fragment), and then passed through to the booking provider. The exact place where this is configured for SHRSS must be confirmed in your component specifications. |  | Adobe | Pending | FALSE | 45 |
| Locations | Is booking engine logic  currently limited to specific engines (e.g., SynXis)? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. The booking widget/engine is a custom integration: in many implementations, booking URLs or IDs are stored in component dialog fields or in a linked data source, and then passed through to the booking provider. Whether SHRSS supports only certain engines today is a design/contract decision, not an AEM limitation. |  | Adobe | Pending | FALSE | 45 |
| Locations | If additional booking engines  are required, does that require component enhancement? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. Supporting additional booking engines typically requires enhancements to the booking components and/or integration layer to handle new parameters, URLs, or APIs. This is not something AEM adds automatically. |  | Adobe | Pending | FALSE | 45 |
| Locations | Should visual alignment issues  in the booking widget be logged as a gap? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. From a governance standpoint, visual and behavioral changes to shared booking components should go through your normal defect/enhancement triage process. Whether something is treated as a defect or a gap is a SHRSS product/UX decision, not an AEM product rule. |  | Adobe | Pending | FALSE | 45 |
| Locations | Where should A/B testing  (Target) be applied — at Experience Fragment level or elsewhere? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. A/B testing and personalization are typically orchestrated in Adobe Target using experiences or activities defined against the Locations templates/components. From an AEM perspective, you would expose stable CSS selectors/DOM structure, and then Target decides what to vary. The exact placement and scope of tests for Locations should be defined in your optimization plan rather than in the AEM components themselves. |  | Adobe | Pending | FALSE | 45 |
| Locations | Is analytics event firing  handled in the component or at developer/data layer level? |  |  | This behavior is implementation-specific to the SHRSS Locations solution, which relies on custom integration between AEM and DPLT plus custom map/booking components. Typically, analytics for locations (clicks, map interactions, booking starts, etc.) are implemented via a data layer and Adobe tags/Analytics/Target. Whether specific events fire for these components depends on how the front-end JavaScript and tags were implemented for SHRSS and should be validated in your analytics specification and tag configuration. |  | Adobe | Pending | FALSE | 45 |