
| Session | Question | Date Asked | Asked By | Answer | Answered On | Answered By | Status | AI Generated | Confidence |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Events | What fields in the card and detail page are managed by status values? |  |  | Which card/detail fields respond to status (for example, badge text, CTA label, or visibility) is defined in the custom Event card and detail components and the Event Content Fragment model. AEM does not prescribe those status rules out of the box; they are implementation‑specific for SHRSS and should be documented in the component specifications. |  | Adobe |  | TRUE | 65 |
| Events | Can hot events be scheduled to appear on a specific date? |  |  | Complex scheduling such as “hot” or featured events appearing on specific dates is not provided by a generic AEM events module. Out of the box, AEM lets you schedule **page** activation/deactivation and Content Fragment publication, but linking that directly to a custom “hot event” state is implementation‑specific for the SHRSS build and must be confirmed in your project design and workflows. |  | Adobe |  | TRUE | 65 |
| Events | Can hot events be prioritized to appear at the top of the event listing grid? |  |  | Prioritizing “hot” events to the top of a listing requires custom sort logic in the event listing component (for example, sorting by a flag or status field before date). AEM provides query APIs and core list components that can be extended, but any special ordering for hot events in SHRSS is implementation‑specific and depends on how the listing component was built. |  | Adobe |  | TRUE | 65 |
| Events | How does the \"Show all events\" view prioritize events when multiple properties share the same event? |  |  | AEM does not have a standard rule for prioritizing events across properties; “Show all events” behavior is determined entirely by the custom query and sort logic used by the SHRSS event listing (for example, sort by date, then by a primary property, then alphabetically). You’ll need to refer to the implementation design or code to see exactly how priorities are resolved when the same event appears for multiple properties. |  | Adobe |  | TRUE | 65 |
| Events | How do multi-venue events and multi-date ranges appear in the cards and details page? (e.g., does it list all venues/dates or just a rolled-up summary?) |  |  | The way multi‑venue and multi‑date events are rendered (showing all venues/dates vs. a summarized view) is defined by the Event Content Fragment model and the custom card/detail components. AEM does not enforce a particular display pattern here; the SHRSS implementation decides how those fields are mapped and displayed, so you’ll need to check the component design and authoring guidelines for the exact behavior. |  | Adobe |  | TRUE | 65 |
| Events | How do we manage event-specific SEO fields (Meta Title, Meta Description, Open Graph, Schema Event markup)? |  |  | In AEM, SEO metadata such as **Meta Title**, **Meta Description**, and Open Graph tags are typically managed via **page properties** on the event detail page and rendered in the `<head>` of the page template [Page Properties](https://experienceleague.adobe.com/en/docs/experience-manager-cloud-service/content/sites/authoring/sites-console/page-properties). You can also expose SEO fields on the Event Content Fragment (CF) model and have the detail component map those values into page metadata. Schema.org **Event** markup is not generated automatically; it is usually implemented as custom JSON‑LD in the event detail component that reads from CF fields in the SHRSS implementation. |  | Adobe |  | TRUE | 60 |
| Events | How do we manage the hierarchy of the \"All Events\" page under each category and/or subcategory in terms of URL structure and consumption by search engines? |  |  | Event listing and category URLs in AEM are defined by your **site structure and templates**, not by a fixed product convention. Best practice is to place event listings under a stable path (for example, `/events/category/…`) and use AEM’s sitemap capability so those URLs are included for SEO [SEO and URL Management Best Practices for Adobe Experience Manager as a Cloud Service](https://experienceleague.adobe.com/en/docs/experience-manager-cloud-service/content/overview/seo-and-url-management), [Sitemaps](https://experienceleague.adobe.com/en/docs/experience-manager-learn/sites/seo/sitemaps). Exactly which paths/categories SHRSS uses, and how “All Events” pages are wired into navigation, are implementation‑specific and should follow your IA and SEO strategy. |  | Adobe |  | TRUE | 60 |
| Events | How do filters affect the \"Show all events\" vs. property-specific listings? |  |  | How filters are applied to “Show all events” versus property‑specific listings is controlled entirely by the custom Events filter/listing components. AEM’s query APIs can be scoped to one site subtree or to multiple properties, but whether a filter applies globally or only within a property in the SHRSS build depends on how those components were implemented and configured. |  | Adobe |  | TRUE | 60 |
| Events | How do we configure event lifecycle (Announcement, Presale, On-Sale, Post-Event, Cancelled, Rescheduled) and associated timing rules? |  |  | AEM does not ship with a predefined event lifecycle; states such as Announcement, Presale, On‑Sale, etc. are typically modeled as fields on the Event Content Fragment or page and then interpreted by custom components and workflows. Any timing rules that change badges, CTAs, or visibility based on dates/status values are implementation‑specific for SHRSS and must be documented as part of the event model and component logic. |  | Adobe |  | TRUE | 65 |
| Events | How do we configure the fields in the Events filter component (Venues, Categories, Dates, Properties) and their relationship to tags/metadata? |  |  | Filter options such as venue, category, date presets, and property are normally wired to underlying **tags or metadata fields** on Event Content Fragments or pages. However, AEM does not provide a generic event filter; which fields are exposed, how they map to tags or properties, and how they are combined (AND/OR) in queries for SHRSS are all implementation‑specific decisions defined in the event filter component design. |  | Adobe |  | TRUE | 65 |
| Events | What is the relationship between Location References and filter options in the \"All Events\" view? |  |  | Any relationship between Location References and filter options (for example, only showing venues that have events in the current view) is determined by the custom Events filtering logic. AEM can query on referenced Content Fragments and tags, but whether Location References drive filter options or just card content in the SHRSS “All Events” view is implementation‑specific and needs to be confirmed against the component requirements. |  | Adobe |  | TRUE | 60 |
| Events | Can we configure the filters differently for different properties or event types (e.g., different filter sets for US vs International properties)? |  |  | Yes, it is technically possible to vary filter configurations per site/property or per event type, usually via **component dialog settings, policies, or context‑aware configurations**. However, whether the SHRSS Events filters currently support per‑property or per‑event‑type configurations is implementation‑specific and depends on how those components and policies were built. |  | Adobe |  | TRUE | 60 |
| Events | How do we handle filters for multi-venue events that span multiple properties or brands? |  |  | Support for multi‑venue events across properties (for example, one event tagged to multiple venues/brands and still behaving intuitively in filters) is not provided by a generic AEM feature; it must be handled by the custom event data model and filter query logic. For SHRSS, the exact behavior needs to be defined in the event model (how venues/properties are stored) and implemented in the filter component’s query predicates. |  | Adobe |  | TRUE | 60 |
| Events | How does the system handle overlapping events with the same venue but different properties or brands? |  |  | Handling overlapping events at the same venue across multiple properties/brands (for example, deduplication, display ordering, or conflict rules) is entirely implementation‑specific. AEM’s repository can store all of these events, but how they are surfaced or de‑duplicated in SHRSS listings is determined by your custom queries and business rules, not by an out‑of‑the‑box behavior. |  | Adobe |  | TRUE | 60 |
| Events | How do we manage access controls for event authoring across properties to prevent cross-property editing errors? |  |  | In AEM, role‑based permissions are managed via **groups and ACLs** on content and asset paths. To avoid cross‑property editing, SHRSS should define separate author groups per brand/property and restrict their access to the relevant `/content` and `/content/dam` subtrees; you can also scope Content Fragment model usage per folder [Managing Content Fragments](https://experienceleague.adobe.com/en/docs/experience-manager-cloud-service/content/assets/content-fragments/content-fragments-managing). Any additional safeguards in event components (for example, restricting property pickers) are implementation‑specific and should align with this permission model. |  | Adobe |  | TRUE | 60 |
| Events | Can we set up role-based author permissions per property for event creation and management? |  |  | Yes. AEM supports **role‑based permissions per site/property** by assigning authors to groups and applying ACLs to specific branches in `/content` and `/content/dam`. For SHRSS, you would typically have per‑property author groups with permissions only on their own event content and CF folders. The detailed group structure and permission assignments, however, are part of your implementation and environment configuration. |  | Adobe |  | TRUE | 60 |
| Events | How do we manage governance around event data fields to ensure consistency (e.g. required fields, validation rules, character limits)? |  |  | Governance for event data fields in AEM is typically implemented via **Content Fragment Model constraints** (required fields, allowed value types, regex, max lengths) and, if needed, **workflows** that block publication until required data is present [Working with Content Fragments - Concepts and Best Practices](https://experienceleague.adobe.com/en/docs/experience-manager-cloud-service/content/sites/administering/content-fragments/overview). Which fields are required for SHRSS events, and how strictly they are validated before publishing, are implementation‑specific and should be documented in your CF models and governance guidelines. |  | Adobe |  | TRUE | 60 |
| Events | How are event data changes tracked and audited across versions for regulatory compliance? |  |  | AEM provides **versioning** and a timeline for pages and Content Fragments, allowing you to see changes and roll back to previous versions [Authoring Content Fragments in AEM](https://experienceleague.adobe.com/en/docs/experience-manager-learn/sites/content-fragments/content-fragments-feature-video-use). Out of the box, this offers basic auditability of event content changes. If SHRSS requires formal regulatory audit trails (for example, who changed what field and when with approvals), that typically involves additional workflow steps, audit logs, or integrations and is implementation‑specific. |  | Adobe |  | TRUE | 60 |
| Events | How does the system handle time zone differences for event start/end times, especially for events spanning multiple time zones? |  |  | Handling time zones for event start/end times is driven by how dates/times are stored in the Event Content Fragment and how the event components format them at render time. AEM stores date/time fields as timezone‑aware values, but any logic to normalize times, show local vs venue time, or handle multi‑timezone events is implementation‑specific for SHRSS and should be defined in the event model and presentation requirements. |  | Adobe |  | TRUE | 60 |
| Events | How does on-page search scope work - does it search across event titles, descriptions, tags, and status or only specific fields? |  |  | On‑page search scope (which fields—title, description, tags, status—are searched) is determined entirely by how the event listing/search component builds its query. AEM’s underlying search APIs can query across any indexed fields, but which fields are actually searched for SHRSS events is an implementation‑specific decision reflected in the component’s query configuration and index definitions. |  | Adobe |  | TRUE | 60 |
| Events | How does pagination behave when filters are applied (e.g., does it reset to page 1 or remember the selected page)? |  |  | Pagination behavior under filtering (resetting to page 1 vs. preserving current page) is strictly a front‑end and component design choice. AEM supports either approach via custom logic in the event listing; how SHRSS’s Events listing behaves today is implementation‑specific and comes from the component’s UI/UX design. |  | Adobe |  | TRUE | 60 |
| Events | How do we configure the events filter to support multi-select filters (e.g. Venue + Category + Date Range)? |  |  | AEM does not impose limits on filter combinations; support for multi‑select filters like Venue + Category + Date Range is implemented in the custom Events filter component by combining predicates in the underlying query. Whether SHRSS currently supports multi‑select, and whether those filters are combined with AND or OR semantics, is implementation‑specific and should be confirmed with the component’s functional design. |  | Adobe |  | TRUE | 60 |
| Events | How does the system prevent conflicting filters (e.g., choosing a venue that doesn't have any events in the selected category or date range)? |  |  | Avoiding “conflicting” filters (that yield no results) is handled by component logic, such as disabling incompatible options or showing a “no results” message and suggesting adjustments. AEM doesn’t provide this out of the box for events; any behavior to prevent or mitigate conflicting filter combinations in SHRSS’s Events experience is custom and should be captured in the filter component’s UX requirements. |  | Adobe |  | TRUE | 60 |
| Events | Does the events filter support dynamic filtering logic that adapts to the selected property (e.g. property-specific venues, categories, and dates)? |  |  | Dynamic filters that adapt to the selected property (showing only relevant venues/categories/dates) are achievable using custom query logic and UI code, but they’re not provided by default. Whether SHRSS’s event filter component currently adapts its options by property is implementation‑specific and must be checked in the component behavior and documentation. |  | Adobe |  | TRUE | 60 |
| Events | Are filtered results URL-driven (e.g. query parameters) to support sharable filtered views? |  |  | Whether filtered event results are encoded in the URL (for example using query parameters or hash fragments) is entirely a component‑level choice. AEM supports reading/writing URL parameters in components, but SHRSS’s Events filters will only produce shareable filtered views if that behavior was specifically implemented in the filter UI and routing logic. |  | Adobe |  | TRUE | 60 |
| Events | How does the system handle browser history and back button behavior with filters and pagination applied? |  |  | Browser history and back‑button behavior for filtered/paginated event lists depend on whether the component updates the URL to reflect state and uses client‑side routing. AEM does not enforce a particular pattern; any support for restoring filter and pagination state via the back button in SHRSS’s Events implementation is custom and defined in the front‑end behavior. |  | Adobe |  | TRUE | 60 |
| Events | How do we prevent expired events from appearing in search results or event listings, especially for compliance? |  |  | Preventing expired events from showing in listings/search is normally handled by **query predicates** (for example, comparing end date/status to “now”) plus content governance (unpublishing or archiving events). AEM does not automatically hide expired custom events; the SHRSS event listing and search components must be implemented to exclude expired/archived states according to your compliance requirements. |  | Adobe |  | TRUE | 60 |
| Events | How does the system handle cancelled or rescheduled events - do they remain visible with updated status or get removed from listings? |  |  | Visibility of cancelled or rescheduled events is entirely driven by business rules encoded in the SHRSS event listing and detail components. AEM does not define default behavior for event statuses; your implementation decides whether such events remain visible with updated status, move to a separate view, or are excluded from standard listings while still accessible via direct URL. |  | Adobe |  | TRUE | 60 |
| Events | How do we ensure users can still find cancelled/rescheduled events for reference while keeping active listings clean? |  |  | A common pattern in AEM is to keep cancelled/rescheduled events **published but filtered out** of primary listings, with a dedicated filtered view or tag (for example, “cancelled”) for reference. Implementing this requires custom query logic and possibly separate views in SHRSS’s event components; there is no out‑of‑the‑box mechanism that automatically creates reference‑only views for cancelled events. |  | Adobe |  | TRUE | 60 |
| Events | How do we configure which browsers and versions are supported for the Events experience? |  |  | Supported browsers and versions are defined by SHRSS’s **front‑end support matrix** and documented outside of AEM itself (for example, in project standards). AEM is broadly compatible with modern browsers, but any specific browser/version support policy for the Events experience is an implementation and operations decision, not an AEM product configuration. |  | Adobe |  | TRUE | 55 |
| Events | How do we handle accessibility requirements (WCAG/ADA) for the Events template, including keyboard navigation and ARIA roles? |  |  | AEM’s templates and Core Components are designed to support accessible implementations, but WCAG/ADA compliance depends on how the **Events template and components** are coded (markup, ARIA roles, keyboard focus order, color contrast, etc.). There is no automatic accessibility guarantee; SHRSS must validate the Events experience against WCAG and fix any issues in the front‑end implementation. |  | Adobe |  | TRUE | 60 |
| Events | How do we enforce accessibility best practices for event images (e.g., descriptive alt text, non-decorative images)? |  |  | In AEM, authors can and should maintain **meaningful alt text** on images via asset metadata or component fields. Governance for events typically means: (1) CF or component models require an alt‑text field for key images, and (2) reviews/workflows check for missing or non‑descriptive values. There’s no product‑level enforcement that alt text is “good”; how strictly SHRSS enforces accessibility for event images is a process and implementation decision. |  | Adobe |  | TRUE | 60 |
| Events | How do we handle localization and translation of event details across languages and regions? |  |  | AEM supports localization via **language copies** and **translation projects** for both pages and Content Fragments [Working with Content Fragments - Concepts and Best Practices](https://experienceleague.adobe.com/en/docs/experience-manager-cloud-service/content/sites/administering/content-fragments/overview). For events, you can localize Event CFs by locale-folder and use AEM’s translation workflows. The exact localization model for SHRSS Events (shared CFs vs. per‑locale CFs, translation vendor, workflows) is implementation‑specific and should be defined in your localization design. |  | Adobe |  | TRUE | 60 |
| Events | How do we manage currency and pricing differences across regions for event tickets? |  |  | Currency and pricing logic is normally handled by the **ticketing/commerce system** integrated with AEM, not by AEM itself. AEM can store display values or reference external systems, but any multi‑currency support, price formatting, and regional pricing rules for SHRSS events are implementation‑specific and depend on the external ticketing integration and its configuration. |  | Adobe |  | TRUE | 60 |
| Events | How do we support multiple ticket providers or partner integrations within the Events experience? |  |  | Integrating multiple ticket providers is done via custom components and back‑end services in AEM; there is no generic events/ticketing integration in the product. For SHRSS, how many providers are supported, how they are selected per event, and how their flows are embedded or linked is purely implementation‑specific and should be captured in integration design documentation. |  | Adobe |  | TRUE | 60 |
| Events | How is the responsiveness of the Events template managed for various breakpoints (mobile, tablet, desktop)? |  |  | Responsiveness is handled by the **responsive grid and CSS** of the Events template and components. AEM’s editable templates and Core Components provide responsive foundations, but how SHRSS event pages adapt at each breakpoint (number of columns, stacking behavior, font sizes, etc.) is determined by the front‑end implementation rather than an AEM configuration. |  | Adobe |  | TRUE | 60 |
| Events | How do event cards adapt across breakpoints - does the layout change automatically or is it configured per breakpoint? |  |  | Event card behavior across breakpoints (for example, 1‑column vs 3‑column layouts, image aspect ratios) is implemented in the card component’s CSS and responsive grid configuration. AEM supports breakpoint‑specific styling but does not automatically redesign cards; how SHRSS event cards adapt is defined by the front‑end code and any component style policies. |  | Adobe |  | TRUE | 60 |
| Events | How do we prevent layout shift when images are missing or slow to load (e.g., reserved space, placeholders)? |  |  | Preventing layout shift (CLS) for event images is typically handled by reserving space via fixed‑ratio containers, using placeholders, or setting explicit width/height attributes in the component markup. These are front‑end practices rather than AEM configurations; how SHRSS handles missing or slow images in Events is implementation‑specific and should be addressed in the component HTML/CSS. |  | Adobe |  | TRUE | 60 |
| Events | How do max character limits on titles and descriptions behave across breakpoints without truncating important content? |  |  | Character limits across breakpoints are usually enforced either in the **Content Fragment model** (validation) or via CSS/JS in the component (for example, line clamping and ellipses). AEM does not impose default limits for event titles/descriptions; how SHRSS balances content length against layout constraints is an implementation decision encoded in the CF model and card/detail component design. |  | Adobe |  | TRUE | 60 |
| Events | How do we configure event card layout variations (e.g. featured vs standard cards, horizontal vs vertical layouts)? |  |  | Layout variations for event cards (featured vs standard, horizontal vs vertical) are defined by the custom card components and their style policies. AEM Core Components support variations via policies and style system, but the specific options available to SHRSS authors and how they are selected on the page are implementation‑specific and should be documented in the component configuration. |  | Adobe |  | TRUE | 60 |
| Events | How do orphaned cards behave in the final row of the grid - are they left-aligned, centered, or stretched? |  |  | The behavior of “orphaned” cards in the final grid row is governed by CSS (flexbox/grid alignment) rather than AEM features. AEM simply outputs the markup; how SHRSS’s Events grid aligns 1–2 remaining cards in the last row (left, center, stretched) is controlled by the front‑end implementation and can be adjusted there. |  | Adobe |  | TRUE | 60 |
| Events | How do we manage event templates that contain event-specific fields vs generic fields for different types of events? |  |  | Distinguishing event‑specific vs generic fields is normally done at the **Content Fragment Model** level by defining separate models or optional fields per event type [Content Fragments](https://experienceleague.adobe.com/en/docs/experience-manager-cloud-service/content/sites/authoring/fragments/content-fragments). AEM supports multiple CF models and folder‑based model restrictions. The exact event models SHRSS uses (per type vs unified) and how those are mapped to templates and components are implementation‑specific. |  | Adobe |  | TRUE | 60 |
| Events | How do we manage event categories that represent multiple dimensions (e.g. type, audience, theme) in tagging? |  |  | In AEM, it’s common to model multi‑dimensional classification using **separate tag namespaces** (for example, `events:type`, `events:audience`, `events:theme`) [Administering Tags](https://experienceleague.adobe.com/en/docs/experience-manager-cloud-service/content/sites/administering/tags). This lets you apply multiple tags to the same event CF or page. The specific taxonomy for SHRSS (namespaces, allowed values, governance) is implementation‑specific, but the underlying pattern is to use tags, not hard‑coded fields, for these dimensions. |  | Adobe |  | TRUE | 60 |
| Events | How do we manage event-specific metadata that needs to be shared with external systems (e.g. CRM, marketing automation)? |  |  | Sharing event metadata with external systems (CRM, marketing automation, etc.) typically uses **APIs or export processes** built on top of AEM (for example, via custom servlets, GraphQL, or integration middleware). AEM can expose Content Fragment data as JSON; mapping that to external systems and managing synchronization is implementation‑specific for SHRSS and must be defined in the integration design. |  | Adobe |  | TRUE | 60 |
| Events | How are event lifecycle dates managed when there are multiple phases (e.g. announcement, presale, on-sale, post-event)? |  |  | Multi‑phase lifecycle dates are usually modeled as multiple date fields on the Event Content Fragment (for example, announcement date, presale start, on‑sale start, event end). AEM can store all of these, but how they are interpreted (for badges, visibility, validation) in SHRSS is implemented in the custom event components and workflows, not by a generic product feature. |  | Adobe |  | TRUE | 60 |
| Events | How can we ensure event information is cached appropriately across environments while minimizing stale data? |  |  | Cache behavior for Events is controlled by the **dispatcher/CDN configuration** (cache rules, TTLs, invalidation) and any event‑specific cache‑flush rules. AEM can flush cache on activation, but fine‑grained control (for example, purging specific event pages or JSON endpoints) is an operational and implementation decision for the SHRSS environment; it isn’t managed at the component level. |  | Adobe |  | TRUE | 60 |
| Events | How do we guarantee that event updates reflect within an acceptable caching window (e.g. 5-15 minutes) across Stage and Prod? |  |  | Meeting a specific “time to freshness” SLA (for example, 5–15 minutes) depends on your **publish and cache invalidation strategy**—dispatcher config, CDN TTLs, and cache‑flush triggers—not on an AEM core feature. SHRSS would need to set appropriate TTLs and cache flush behavior for event content to ensure updates are visible within the agreed window in Stage and Prod. |  | Adobe |  | TRUE | 60 |
| Events | How is the performance of event listings monitored and tuned over time for growing data volumes? |  |  | Event listing performance is monitored and tuned using standard AEM/Cloud tooling: request logs, AEM performance dashboards, and any APM the SHRSS team uses. Tuning typically involves optimizing query scopes, adding or adjusting index definitions, and constraining page sizes [Working with Content Fragments](https://experienceleague.adobe.com/en/docs/experience-manager-cloud-service/content/assets/content-fragments/content-fragments). There is no automatic performance tuning for events; it’s an ongoing implementation and operations responsibility. |  | Adobe |  | TRUE | 60 |
| Events | How do we ensure dynamic queries stay performant as the number of events grows over time (e.g., beyond 1000+ active events)? |  |  | Dynamic event queries rely on AEM’s underlying **Oak/Lucene indexes**. To remain performant at scale, queries should be constrained to the events subtree, use indexed properties only, and return limited result sets [SEO and URL Management Best Practices for Adobe Experience Manager as a Cloud Service](https://experienceleague.adobe.com/en/docs/experience-manager-cloud-service/content/overview/seo-and-url-management). Any concrete performance targets (for example, response times at 1,000+ events) must be achieved via index design and load testing in the SHRSS implementation. |  | Adobe |  | TRUE | 60 |
| Events | How are new event categories, venues, and properties added without disrupting existing navigation and filtering? |  |  | Adding new event categories, venues, and properties in AEM is usually done by adding new **tags, CF entries, or site branches**. To avoid disruption, SHRSS should follow a governed process: update tag taxonomies, adjust filter configurations, and test navigation before publishing. There is no automatic protection against UX changes; stability depends on IA governance and how the Events components interpret these new values. |  | Adobe |  | TRUE | 60 |
| Events | How is the \"All Events\" page configured to aggregate events across properties while supporting property-specific views? |  |  | Aggregating events across properties versus showing property‑specific views is controlled by the custom listing component’s query scope (for example, querying a shared CF folder vs. property‑specific folders). AEM provides the repository and query APIs, but the exact “All Events” vs property‑filtered behavior in SHRSS is implementation‑specific and defined in the component configuration and architecture. |  | Adobe |  | TRUE | 60 |
| Events | How do we prevent filter pollution if Location References are incorrectly or inconsistently applied to Events? |  |  | Preventing “filter pollution” from incorrect Location References is mainly a governance and validation issue. In AEM, you can restrict which locations can be selected (for example, using path or tag constraints in dialogs) and add workflow checks. Any additional validation logic that SHRSS uses to enforce correct Location References in Events is implementation‑specific and should be captured in authoring guidelines and validation rules. |  | Adobe |  | TRUE | 60 |
| Events | How do we ensure consistent behavior between the Events experience on the global site and delegated child sites? |  |  | Consistency between global and delegated child sites is typically achieved via **Multi‑Site Management** and shared templates/components. AEM can propagate structure and components across sites, but how SHRSS uses MSM (if at all) and how the Events components are configured in child sites is implementation‑specific and needs to be aligned in your site governance model. |  | Adobe |  | TRUE | 60 |
| Events | How do we handle cases where an event should appear in multiple categories or tags without duplicating content? |  |  | AEM is designed to support this pattern: you create a single Event Content Fragment and apply multiple **tags/categories** to it [Administering Tags](https://experienceleague.adobe.com/en/docs/experience-manager-cloud-service/content/sites/administering/tags). Listing components then query by tags so the same event appears in each relevant category without duplicating content. SHRSS should follow this pattern and avoid copying event records across categories. |  | Adobe |  | TRUE | 60 |
| Events | How do we handle partial-day events or multi-day events that span midnight in calendar views? |  |  | Rendering partial‑day or multi‑day events (especially across midnight) is a responsibility of the Events UI components. AEM simply stores the start/end date/time values; how those are visualized in calendars/timelines in SHRSS (for example, showing each day separately vs. a continuous bar) is implementation‑specific front‑end behavior. |  | Adobe |  | TRUE | 60 |
| Events | How do we ensure that event cards maintain consistent height when titles or metadata differ in length? |  |  | Consistent card height is handled by CSS (for example, setting min‑height, line clamps, or using flexbox/grid alignment). AEM does not automatically normalize card heights; the SHRSS event card component needs to implement the desired behavior in its markup and styles. |  | Adobe |  | TRUE | 60 |
| Events | How do each of the onsite systems sync up to validate what is coming in for the events? |  |  | Synchronization and validation between onsite systems (for example, ticketing, CRM, data warehouse) and AEM are integration concerns outside standard product behavior. Typically, SHRSS would have one or more integration services that push/pull event data via APIs; those flows, validations, and reconciliation checks are entirely implementation‑specific and should be documented in integration architecture and runbooks. |  | Adobe |  | TRUE | 60 |
| Events | Is there a minimum number of days required for the event lifecycle dates to ensure proper display in cards and detail pages? |  |  | Any minimum‑days rule for event lifecycle phases (for example, minimum presale duration) is a business rule that must be implemented in the SHRSS event model and components. AEM does not enforce such constraints by default; if required, they should be encoded as validation logic in the CF model or publish workflows. |  | Adobe |  | TRUE | 60 |
| Events | How do we visually differentiate event states (Announcement, Presale, On-Sale, Post-Event, Cancelled, Rescheduled) in the UI? |  |  | Visual differentiation of event states (badges, colors, labels) is handled by the **card/detail component design and CSS**. AEM does not prescribe how different statuses look; SHRSS must define and implement a clear visual language for each state as part of the Events UX and component styling. |  | Adobe |  | TRUE | 60 |
| Events | How can we prevent events with incomplete governance fields from being published or displayed? |  |  | The usual pattern in AEM is to combine **required fields and validation** in the Content Fragment model or component with **workflow steps** that block activation if required governance fields are missing. For SHRSS, you would define which fields are mandatory, enforce them at authoring time, and, if necessary, add a pre‑publish review workflow for Events. |  | Adobe |  | TRUE | 60 |
| Events | How do we ensure that events with incorrect or inconsistent Location References do not pollute filters on the Events page? |  |  | Avoiding incorrect Location References in filters is achieved through governance and validation: restricting selectable locations in dialogs (for example, to a single branch), using validations on the CF model, and periodic audits. AEM itself will happily index whatever is stored; the responsibility for data quality in SHRSS’s Events lies in how those references are constrained and reviewed. |  | Adobe |  | TRUE | 60 |
| Events | Does the events filter support multi-select filters, combinations of venues, categories, and date ranges, or is it single-select only? |  |  | AEM can support both single‑select and multi‑select filters, but the actual capability depends entirely on the custom SHRSS Events filter component. Whether combinations of venue, category, and date range are supported today is determined by that implementation and is not controlled by a generic AEM configuration. |  | Adobe |  | TRUE | 60 |
| Events | How does the grid layout handle different numbers of events when filters are applied (e.g., odd numbers of cards in a row)? |  |  | Handling “odd” card counts per row is purely CSS/layout behavior in the grid component. AEM outputs the list of events; how the last row looks when the card count isn’t a multiple of the column count is controlled by SHRSS’s front‑end implementation (for example, left‑aligned vs centered last row). |  | Adobe |  | TRUE | 60 |
| Events | How do we configure the number of event cards per row per breakpoint (e.g., 1 per row on mobile, 2 on tablet, 3-4 on desktop)? |  |  | The number of cards per row per breakpoint is defined by responsive grid settings and CSS (and, optionally, component style policies). AEM supports multiple breakpoints, but the specific configuration for SHRSS Events (1‑up on mobile, 2‑up on tablet, etc.) is an implementation choice encoded in the Events template and card component styles. |  | Adobe |  | TRUE | 60 |
| Events | How are event custom fields exposed to filters or search facets? |  |  | Exposing custom event fields to filters or search facets requires that those fields be **indexed** and that the listing/search components use them in queries. AEM can index custom properties, but configuring them as facets or filter options is implementation‑specific in SHRSS and depends on the index definitions and component code. |  | Adobe |  | TRUE | 60 |
| Events | Can filtering logic be centrally managed across different event listing components? |  |  | Centralizing filtering logic is an architectural decision: SHRSS could implement a shared service or model component that all event listings call into. AEM doesn’t enforce this pattern; whether event filtering is centralized or duplicated across components in SHRSS is implementation‑specific and should be addressed in the component architecture. |  | Adobe |  | TRUE | 60 |
| Events | What is the recommended set of standard filters for the Events experience (e.g., venue, date range, category, price, age restriction)? |  |  | From an AEM standpoint there is no enforced “standard” set of filters; recommended filters come from SHRSS’s UX and business needs (for example, venue, date, category, property, audience/age). Technically, all of these can be supported via tags/fields and custom filter components. The canonical filter set should be defined in SHRSS’s information architecture and customer journey mapping rather than the product. |  | Adobe |  | TRUE | 60 |
| Events | How do we accommodate different screen reader behaviors and text scaling settings in the Events template? |  |  | Screen reader support and text scaling are governed by semantic HTML, ARIA attributes, and responsive typography in the Events components. AEM provides the platform, but ensuring that SHRSS’s Events template behaves correctly with assistive technologies and zoom is part of implementing and testing against accessibility guidelines (for example WCAG 2.1 AA). |  | Adobe |  | TRUE | 60 |
| Events | How do we add or remove available filters without causing inconsistencies in previously published events? |  |  | Adding/removing filters usually means changing which tags/fields are surfaced in the filter UI while leaving existing event data intact. From AEM’s side, historical events will still have the old tags/metadata; SHRSS needs to decide whether to migrate/normalize those values or accept that some legacy content may not use new filters. The change management and data migration strategy here is implementation‑specific. |  | Adobe |  | TRUE | 60 |
| Events | How do we ensure dynamic components, such as the Events slider, remain accessible and functional across devices and assistive technologies? |  |  | Sliders and other dynamic components must be built with accessibility in mind (keyboard focus, ARIA roles, pause/stop controls, etc.). AEM does not automatically make sliders accessible; SHRSS’s Event slider implementation needs to follow accessibility best practices and be tested with screen readers and across devices. |  | Adobe |  | TRUE | 60 |
| Events | How do we troubleshoot discrepancies if an event appears in search but not in the Events listing (or vice versa)? |  |  | Troubleshooting this typically involves checking: (1) event status and dates, (2) tags/filters used by the listing, and (3) search index state (for example, whether the content is indexed under the expected path/fields). AEM’s references and search tools help, but any SHRSS‑specific rules (like additional filters on the listing only) need to be reviewed in component configuration and index definitions. |  | Adobe |  | TRUE | 60 |
| Events | How do we configure dynamic queries for \"Show all events\" to stay performant as the number of events increases? |  |  | Keeping “Show all events” performant relies on scoping queries appropriately (to events folders/paths), using indexed properties, and limiting results. AEM allows you to define custom Oak/Lucene index definitions; tuning those indexes and queries for SHRSS’s data volumes is an implementation task rather than an AEM configuration toggle. |  | Adobe |  | TRUE | 60 |
| Events | What is the target SLA for event updates reflecting in Production (e.g., maximum 15 minutes from publish)? |  |  | AEM as a Cloud Service does not define a built‑in SLA for how quickly individual content changes appear; that is governed by your deployment, cache strategy, and operations. Any target such as “events reflect in ≤15 minutes” for SHRSS must be defined as part of your operational SLAs and achieved through dispatcher/CDN configuration and cache‑flush processes. |  | Adobe |  | TRUE | 60 |
| Events | How do we test events in lower environments (Dev, QA, Stage) before promoting changes to Production? |  |  | Standard practice with AEM is to create or copy event content in lower environments and test it end‑to‑end (authoring, listing, detail pages, search/filtering) before promoting via Cloud Manager pipelines to Production. The exact test data strategy, promotion cadence, and sign‑off process for SHRSS Events are implementation/ops decisions rather than product features. |  | Adobe |  | TRUE | 60 |
| Events | How do we coordinate event content changes with external marketing campaigns to avoid mismatched information? |  |  | Coordinating event content with external campaigns is primarily a process issue: typically SHRSS would align publish times, use approvals/workflows in AEM, and ensure campaigns reference canonical URLs in AEM. AEM supports scheduled activations and workflows, but the broader coordination with off‑platform campaigns is outside standard product behavior and should be defined in SHRSS’s operational playbooks. |  | Adobe |  | TRUE | 60 |
| Events | How do we align the AEM Events experience with the broader SHRSS digital strategy and governance model? |  |  | Alignment with SHRSS’s digital strategy and governance happens through IA, design, and governance artifacts rather than AEM configuration alone. AEM provides flexible content modeling and templating; how Events fit into brand hierarchy, governance forums, and KPI measurement is a strategic question that needs to be answered in SHRSS’s broader digital strategy, not within the product. |  | Adobe |  | TRUE | 60 |
| Events | How do we maintain documentation for event authoring workflows and best practices? |  |  | Documentation is typically maintained in SHRSS’s own knowledge base (for example, Confluence/SharePoint) and should cover Event CF models, templates, filter usage, and workflows. AEM does not generate this documentation automatically; it’s up to the SHRSS team to capture and update event authoring runbooks and KT materials as the implementation evolves. |  | Adobe |  | TRUE | 60 |
| Events | Can hot events be scheduled to disappear after a certain date? |  |  | Automatically demoting or hiding “hot” events after a certain date requires custom logic (for example, using a date field on the Event CF and filtering or changing badges based on it). AEM does not provide a native “hot until date” concept; any such behavior in SHRSS is implementation‑specific and should be documented in the event lifecycle rules. |  | Adobe |  | TRUE | 60 |
| Events | Can events be shared across multiple brands or properties while maintaining brand-specific presentation? |  |  | Yes, a common AEM pattern is to have a **single Event Content Fragment** reused across multiple sites/properties, with each site providing its own presentation (templates, components, styling). The mechanics of how SHRSS shares event content and applies brand‑specific presentation are implementation‑specific and depend on your site structure and component design. |  | Adobe |  | TRUE | 60 |
| Events | How do we configure event visibility across properties and the \"Show all events\" view? |  |  | Event visibility across properties is determined by the query scope and filters used by listing components (for example, limiting to a property’s branch vs querying a shared event CF folder with a property flag). AEM can support either model; the exact rules for SHRSS’s “Show all events” vs property‑specific listings are implementation‑specific and defined in the component queries and data model. |  | Adobe |  | TRUE | 60 |
| Events | How do we ensure that the Events page displays consistent messaging and visual hierarchy across different brand experiences? |  |  | Consistent messaging and visual hierarchy are achieved through shared templates, components, and design systems, not through a specific AEM feature. AEM allows SHRSS to share or specialize templates per brand; ensuring consistency across brands requires design governance and template/component reuse aligned with your brand guidelines. |  | Adobe |  | TRUE | 60 |
| Events | How do we validate events with external data sources to ensure accuracy (e.g., ticketing systems, CRM)? |  |  | Validating events against external systems (ticketing, CRM) is typically done via integration logic or periodic reconciliation jobs; AEM does not automatically cross‑check custom events with external data. For SHRSS, any validation pipeline or monitoring comparing AEM event data to source systems is implementation‑specific and part of your integration design. |  | Adobe |  | TRUE | 60 |
| Events | How do we ensure that the Events experience aligns with SHRSS’s legal and compliance requirements (e.g., disclaimers, terms of use)? |  |  | Legal/compliance alignment is handled by including required disclaimers, legal text, and links in the Events templates/components and by governing who can publish event content. AEM provides templates and workflows, but the actual compliance requirements and their implementation for SHRSS’s Events experience must be defined with your legal/compliance stakeholders and encoded in your templates and processes. |  | Adobe |  | TRUE | 60 |
| Events | How do we ensure event descriptions maintain editorial quality & depth (i.e. long \"Description\" field vs smaller \"Body Copy\" field)? |  |  | Editorial quality is governed by SHRSS’s content standards, not AEM itself. From a product perspective, you can model multiple text fields (for example, long description vs short body copy) in the Event CF model and provide guidance on when to use each. Enforcement of style, tone, and depth relies on training, reviews, and potentially workflows, rather than a built‑in AEM rule. |  | Adobe |  | TRUE | 60 |
| Events | Is there version history specific to Content Fragments for event rollback? |  |  | Yes. AEM Content Fragments, including Event fragments, have **version history**—authors can create, compare, and restore previous versions from the Content Fragment editor or Assets/Content Fragments console [Authoring Content Fragments in AEM](https://experienceleague.adobe.com/en/docs/experience-manager-learn/sites/content-fragments/content-fragments-feature-video-use), [Working with Content Fragments](https://experienceleague.adobe.com/en/docs/experience-manager-cloud-service/content/assets/content-fragments/content-fragments). This allows you to roll back an individual event’s content without impacting other content. |  | Adobe |  | TRUE | 75 |
| Events | How does the system handle event series or recurring events (e.g., weekly tours, monthly lectures)? |  |  | Support for recurring or series events (weekly, monthly, etc.) is not provided by a generic events module in AEM; it must be modeled (for example, series identifier + individual occurrences) and implemented in SHRSS’s event CFs and listing logic. Whether SHRSS treats these as separate events, a series with child instances, or something else is implementation‑specific. |  | Adobe |  | TRUE | 60 |
| Events | Is the sitemap updated instantly when an event is published/unpublished, or is there a delay? |  |  | AEM’s Sitemap feature can be configured to generate sitemaps **on demand** or **on a schedule**; when scheduled, there is a delay until the next generation run [Sitemaps](https://experienceleague.adobe.com/en/docs/experience-manager-learn/sites/seo/sitemaps), [SEO and URL Management Best Practices for Adobe Experience Manager as a Cloud Service](https://experienceleague.adobe.com/en/docs/experience-manager-cloud-service/content/overview/seo-and-url-management). For SHRSS, whether event detail pages appear/disappear “instantly” in the sitemap depends on your sitemap generator configuration and dispatcher/CDN caching. |  | Adobe |  | TRUE | 70 |
| Events | How does the system handle exceptions or errors when events cannot be retrieved from the backend data source? |  |  | Error handling when events cannot be retrieved from backend sources (for example, an API outage) is defined by the custom integration and components—typical patterns include fallback messaging, partial data display, or hiding affected modules. AEM itself does not prescribe the behavior; SHRSS’s event and integration components need explicit error handling logic for these scenarios. |  | Adobe |  | TRUE | 60 |
| Events | How does the system handle empty filter states or no event results? |  |  | Handling empty filter states (no results) is governed by the event listing component UX—commonly showing a “no events found” message and possibly suggestions to adjust filters. AEM doesn’t provide a special built‑in events empty‑state; SHRSS must define and implement the desired behavior in the Events listing component. |  | Adobe |  | TRUE | 60 |
| Events | Need to demonstrate cross-property/site reuse (e.g. Authors can see reused events and plan accordingly) |  |  |  |  |  |  | TRUE | 55 |
| Events | How do we set up governance rules around who can create, edit, and publish events and related content? |  |  | Governance is implemented through **AEM permissions (groups/ACLs)** combined with workflows. SHRSS should define which roles can create/edit Event CFs, which can publish event pages, and whether approval is required before activation. AEM supports this pattern, but the exact role definitions and approval flows are defined in SHRSS’s governance model, not by the product. |  | Adobe |  | TRUE | 60 |
| Events | How do we handle changes to event governance rules over time without disrupting existing content? |  |  | Changing governance rules over time typically involves updating group memberships, ACLs, and workflows; existing content remains intact, but who can edit/publish may change. AEM doesn’t retroactively rewrite content based on new rules; SHRSS will need to manage any transitions (for example, reassigning ownership) as part of governance change management. |  | Adobe |  | TRUE | 60 |
| Events | How do we implement approval workflows for event creation and updates? |  |  | AEM supports **custom workflows** that can be triggered on page or asset (including Content Fragment) changes. For Events, SHRSS can implement a workflow that routes new or updated Events for review and requires approval before publishing. The specific steps, approver roles, and criteria are implementation‑specific and should be defined in your governance design. |  | Adobe |  | TRUE | 60 |
| Events | How do we audit and log event publication decisions for compliance and analysis? |  |  | Basic auditing is available via AEM’s audit logs and version history, which record who changed content and when. For more detailed publication decision tracking (for example, approval comments, policy references), SHRSS would need to capture that information in workflow steps or external logging/BI systems. This is an implementation‑specific extension on top of AEM’s standard logs and versions. |  | Adobe |  | TRUE | 60 |
| Events | How do we implement monitoring and alerting around failed event publication or indexing? |  |  | Monitoring and alerting are handled via AEM/Cloud logs, Cloud Manager, and any external APM/monitoring tools SHRSS uses. You can set alerts on specific error patterns (for example, event index failures or activation errors). AEM doesn’t ship event‑specific alerts out of the box; these have to be configured as part of your implementation’s operational monitoring strategy. |  | Adobe |  | TRUE | 60 |
| Events | How is the canonical URL determined for the Events page and each event detail page? |  |  | Canonical URLs are normally defined at the **page level** and rendered via the page component’s `<head>` markup [SEO and URL Management Best Practices for Adobe Experience Manager as a Cloud Service](https://experienceleague.adobe.com/en/docs/experience-manager-cloud-service/content/overview/seo-and-url-management). For SHRSS, event detail pages’ canonical URLs would typically be their published page paths; if any alternate/canonical logic exists (for example, for multi‑brand reuse), it is implementation‑specific in the page template or head component. |  | Adobe |  | TRUE | 65 |
| Events | How do we ensure that the Events URL structure supports future expansions, such as new categories, venues, or properties? |  |  | Future‑proofing URL structure is primarily an IA and design concern: SHRSS should choose stable, hierarchical paths (for example, `/events/{category}/…`) and avoid encoding volatile attributes in URLs. AEM supports refactoring site structures, but changes can affect SEO and links; planning a flexible but stable URL pattern up front is the best practice, not a product setting. |  | Adobe |  | TRUE | 60 |
| Events | How does URL generation handle special characters, spaces, or non-ASCII characters in event titles? |  |  | AEM generates page URLs from the **page name**, applying normalization rules such as lowercasing, replacing spaces with hyphens, and stripping unsupported characters. Non‑ASCII characters are usually transliterated or removed depending on configuration. For SHRSS, any additional naming rules (for example, property codes in URLs) must be enforced via governance or custom logic at page/CF creation time. |  | Adobe |  | TRUE | 65 |
| Events | Does URL generation follow a strict naming convention or does it allow flexibility for authors? |  |  | By default, AEM allows authors some flexibility: the page name (which drives the URL) is set at creation time and can differ from the Title. Many implementations, however, adopt naming conventions and possibly tooling to guide authors. For SHRSS Events, whether authors can freely name URLs or must follow strict patterns is determined by your governance and any custom creation wizards or validations. |  | Adobe |  | TRUE | 60 |
| Events | Can the Events URL be edited after creation without breaking bookmarks or SEO? |  |  | AEM allows renaming/moving pages, which changes their URLs. Internal AEM references are updated automatically, but external links/bookmarks and search engine indexes will see the old URL as changed. Best practice for SHRSS is to configure **redirects** (for example, via dispatcher/CDN or AEM Redirect Map) whenever event URLs are changed to avoid breaking bookmarks and SEO. |  | Adobe |  | TRUE | 70 |
| Events | Does the Events template enforce accessibility standards by default (WCAG/ADA etc)? (it appeared there might be a few accessibility issues) |  |  | AEM does not automatically enforce WCAG/ADA standards on custom templates. Core Components and editable templates are designed to support accessible implementations, but whether the SHRSS Events template meets accessibility standards depends on how its markup and styles were implemented and tested. Any issues observed should be addressed in the component/template code rather than expecting a built‑in enforcement mechanism. |  | Adobe |  | FALSE | 49 |
| Events | Does the Events template enforce a consistent headline hierarchy and page structure? (in short, on Wednesday evening, they looked weird) |  |  | Headline hierarchy and page structure are controlled by the Events template and components, not by AEM core services. AEM will render whatever heading levels and structure the SHRSS implementation defines; if the hierarchy appeared inconsistent, that should be corrected in the template/component HTML and authoring guidelines. |  | Adobe |  | FALSE | 49 |
| Events | Is there a version rollback at the page level as well, or does it apply only to Content Fragments? |  |  | Yes. AEM supports **page versioning and rollback** via the Sites console for all pages, including event listings and detail pages. Authors can create, compare, and restore previous versions of pages independently of Content Fragment versions, enabling rollback at both page and CF levels [Page Properties](https://experienceleague.adobe.com/en/docs/experience-manager-cloud-service/content/sites/authoring/sites-console/page-properties). |  | Adobe |  | TRUE | 70 |
| Events | How can we preview \"future state\" scheduled content for events that are not yet published or scheduled for a future date? |  |  | AEM offers features like **Timewarp** and Launches to preview time‑based content states, but using them for complex event lifecycles depends on how event pages and scheduling are set up. There isn’t an out‑of‑the‑box “preview custom lifecycle state” feature; any use of Timewarp/Launches for SHRSS Events should be designed and tested specifically for your event scheduling model. |  | Adobe |  | TRUE | 60 |
| Events | How do we handle custom filtering logic that depends on a combination of tags, metadata, and status fields? |  |  | Custom filtering logic that combines tags, metadata, and status fields is implemented in the SHRSS event listing component’s query logic. AEM’s query APIs support complex predicates, but the exact rules (how fields are combined and prioritized) are entirely implementation‑specific and should be defined in the component requirements. |  | Adobe |  | TRUE | 60 |
| Events | How do we ensure that event filtering reflects real-time data from external ticketing or CRM systems? |  |  | Real‑time reflection of external data requires integrations that update AEM’s event content or call external services at render time. AEM does not natively synchronize custom event data in real time; for SHRSS, any near‑real‑time filtering based on ticketing/CRM status must be implemented as part of your integration architecture and may involve tradeoffs with performance and caching. |  | Adobe |  | TRUE | 60 |
| Events | How do we ensure that event listings do not expose internal-only or draft events to the public? |  |  | Preventing internal/draft events from appearing publicly is usually handled by a combination of **status fields**, permissions, and publish workflows. For example, SHRSS can ensure only events in a “Ready/Published” status are activated and that the listing queries filter out draft/internal statuses. AEM supports this pattern but doesn’t enforce it automatically; it must be implemented in your event governance and components. |  | Adobe |  | TRUE | 60 |
| Events | How do we ensure events appear equally supported by SEO, consistent metadata strategy, and usage of canonical URLs? |  |  | Ensuring SEO consistency requires SHRSS to define a standard metadata strategy (titles, descriptions, tags, canonical URLs, schema) and encode it in templates/components and authoring guidelines [SEO and URL Management Best Practices for Adobe Experience Manager as a Cloud Service](https://experienceleague.adobe.com/en/docs/experience-manager-cloud-service/content/overview/seo-and-url-management). AEM provides the mechanism; consistent application across all events is a governance and implementation task. |  | Adobe |  | TRUE | 60 |
| Events | Does AEM automatically generate structured data for the Event schema, or must that be custom implemented? |  |  | AEM does not automatically generate schema.org **Event** structured data for custom event implementations. While some Core Components include structured data support, event‑specific JSON‑LD must typically be custom implemented in the event detail component for SHRSS, using fields from the Event Content Fragment and page metadata. |  | Adobe |  | TRUE | 75 |
| Events | How do we ensure events with special access requirements (e.g., members-only events, invite-only events) are not indexed publicly? |  |  | Preventing special‑access events from entering public indexes involves a combination of **access control**, sitemap configuration, and robots/meta directives. Typically, such pages are either not published, blocked from sitemaps, or marked with `noindex` where appropriate. For SHRSS, the specific pattern for members‑only/invite‑only events must be defined and implemented in templates and publishing rules. |  | Adobe |  | TRUE | 60 |
| Events | How do we ensure that events with limited seating or capacity constraints have accurate real-time availability displayed? |  |  | Real‑time capacity/availability is usually provided by the underlying ticketing/booking system rather than AEM. AEM can embed or call that system to show current availability, but any guarantee of accuracy depends on the external integration. For SHRSS, real‑time availability display is implementation‑specific and must be designed with the ticketing provider. |  | Adobe |  | TRUE | 60 |
| Events | How do we ensure visitor privacy when events collect personal data for registration or ticketing? |  |  | Visitor privacy in event flows is primarily governed by your **registration/ticketing systems** and legal policies (consent, data retention, etc.). AEM can host forms or link/redirect to external systems, but ensuring privacy compliance (for example, GDPR/CCPA) for SHRSS events requires proper data handling and legal review in those integrated systems, not just AEM configuration. |  | Adobe |  | TRUE | 60 |
| Events | Does a new property automatically inherit the existing events tag taxonomy? |  |  | By default, AEM tags are **global** across the instance and can be reused by any site/property [Administering Tags](https://experienceleague.adobe.com/en/docs/experience-manager-cloud-service/content/sites/administering/tags). So a new SHRSS property can use the existing events tag taxonomy without additional setup. Any decision to restrict or segment tag usage by property must be implemented via permissions, dialogs, and governance. |  | Adobe |  | TRUE | 75 |
| Events | How do we ensure that event data remains consistent between AEM and external systems like Workday or CRM platforms? |  |  | Consistency between AEM and external systems (Workday, CRM, etc.) depends on your integration patterns (for example, AEM as consumer, producer, or both) and any synchronization/monitoring built around them. AEM does not automatically reconcile event data with external platforms; for SHRSS this requires implementation‑specific sync jobs, APIs, or middleware and clear ownership of the “system of record” for events. |  | Adobe |  | TRUE | 60 |
| Events | Are renditions automatically generated per event component use case (social/profile card vs community calendar listing vs event detail page)? |  |  | When an image asset is uploaded, AEM generates standard renditions (and optional Dynamic Media derivatives) automatically, but **which rendition** is used in an event card/detail/social context is determined by the SHRSS component implementation and its responsive image strategy. There is no event‑specific rendition generation per use case by default; this must be configured in your components and asset setup. |  | Adobe |  | TRUE | 70 |
| Events | What happens if an asset is deleted or moved - does the event card/detail page show a placeholder or error? |  |  | If an image asset is **moved** within AEM, references are updated automatically and event cards/detail pages continue to work. If an asset is **deleted**, the reference remains but the image will no longer resolve, which may appear as a broken image unless the component provides a fallback. Best practice for SHRSS is to avoid deleting in‑use assets and to configure sensible placeholders in the Events components. |  | Adobe |  | TRUE | 70 |
| Events | How do we configure the default fallback image per site per event type? |  |  | Default fallback images are typically configured via component policies (design dialog) or coded defaults that kick in when no image is set. AEM does not provide an automatic per‑site/per‑event‑type fallback; SHRSS would need to implement this in the event components (for example, policy‑driven defaults or a site‑level configuration) and document how authors can control or override them. |  | Adobe |  | TRUE | 65 |
| Events | What is the recommended AEM modeling approach to include all the required fields (description, price, fees, dates, times, venues, references, etc.) and avoid template duplication when we want different visual presentations? |  |  | The typical AEM pattern is to model events as **Content Fragments** with all required structured fields (dates, venue, pricing, references, etc.) and then use multiple templates/components to present that content differently (cards, detail pages, calendars) [Working with Content Fragments - Concepts and Best Practices](https://experienceleague.adobe.com/en/docs/experience-manager-cloud-service/content/sites/administering/content-fragments/overview). This avoids duplicating templates for each variation of content structure; SHRSS can then vary presentation via components and style policies while keeping a single canonical event model. |  | Adobe |  | TRUE | 70 |
| Events | How does AEM handle multi-step navigation patterns that surface in-to-around-through-event management experiences? |  |  | Multi‑step navigation patterns (for example, in‑to‑around‑through event flows) are UX design and front‑end implementation concerns, not product constraints. AEM can host multi‑step pages, wizards, or SPA‑style experiences, but how SHRSS designs and implements these flows for event management is entirely implementation‑specific. |  | Adobe |  | TRUE | 60 |
| Events | How does AEM support authors in understanding hierarchical event relationships (e.g., parent event, child sessions)? |  |  | AEM supports hierarchical relationships via **Content Fragment references** and folder structures [Analyzing Content Fragment Structure](https://experienceleague.adobe.com/en/docs/experience-manager-cloud-service/content/sites/administering/content-fragments/analysis). For events, SHRSS can model parent/child relationships (for example, main event with session CFs) and allow authors to navigate them using the Content Fragment editor’s structure tree and “References” tools. The exact modeling pattern is an implementation choice. |  | Adobe |  | TRUE | 65 |
| Events | Are there authoring guidances in AEM that guide authors on how to abstract structured output from event CFs? |  |  | AEM itself doesn’t enforce authoring guidance beyond what you define in CF models (field names, descriptions, constraints). Best practice for SHRSS is to supplement this with training materials and in‑product help (field descriptions, tooltips) explaining how structured event fields map to outputs (cards, detail, JSON, etc.). |  | Adobe |  | TRUE | 60 |
| Events | Is there a recommended event content hierarchy pattern (e.g., property > category > event type > specific event)? |  |  | AEM does not prescribe a single event hierarchy; common patterns use a combination of **site structure** (for brand/property) and **tags** (for category, event type, audience). For SHRSS, the recommended hierarchy should come from your IA and content strategy, using tags and CF models to avoid over‑nesting page structures while still supporting navigation and filtering [Administering Tags](https://experienceleague.adobe.com/en/docs/experience-manager-cloud-service/content/sites/administering/tags). |  | Adobe |  | TRUE | 60 |
| Events | What is the recommended AEM pattern for cross-site event aggregation and reuse? |  |  | A recommended pattern is to maintain a **shared Events CF folder** (with a common model) and reuse those CFs across multiple sites via Content Fragment components or headless APIs. Listings on each site are then driven by filters (tags, properties) rather than duplicate content. How SHRSS implements cross‑site aggregation (for example, a global “All Events” vs property‑specific views) is an implementation decision leveraging this pattern. |  | Adobe |  | TRUE | 65 |
| Events | What is Adobe’s recommended AEM pattern to support an Events experience that can flex to support needs like performances at shared venues, multi-day weekend festivals, and other complex experiences? |  |  | Adobe generally recommends modeling events as **rich Content Fragments** (with separate fields for dates, venues, properties, pricing, and relationships to sessions/series) and using flexible listing/detail components that query and render those CFs [Content Fragments](https://experienceleague.adobe.com/en/docs/experience-manager-cloud-service/content/sites/authoring/fragments/content-fragments). The specific patterns for shared venues, multi‑day festivals, and complex events for SHRSS, however, must be designed in your solution architecture and are not dictated by a generic product blueprint. |  | Adobe |  | TRUE | 65 |
| Events | Are there standards or policies we should use to evaluate coverage and extensibility of the Events experience versus SHRSS needs? |  |  | Evaluating coverage and extensibility is typically done via architectural and UX reviews rather than a specific AEM feature. For SHRSS, that means assessing whether the current Event CF model, templates, and components cover all required use cases (jobs‑to‑be‑done) and can evolve without breaking changes. AEM provides flexible models and components; the standards/policies you use for evaluation are part of your internal governance and architecture practice. |  | Adobe |  | TRUE | 60 |
| Events | For the shared data use cases and evolving tools like job families, how do we express differentiated experiences between cross-property shares of events in AEM? |  |  | Differentiated experiences for shared content are usually achieved by reusing the same underlying Event CF across properties while using property‑specific templates, components, and styling. AEM’s flexibility supports this pattern; how SHRSS expresses those differences (for example, layout, messaging, ancillary content per property) is implementation‑specific and should be defined in your multi‑property design. |  | Adobe |  | TRUE | 60 |
| Events | How can we design the AEM data model to support the three semantically distinct scopes identified in the RFP (Category, Source, Site-Specific)? |  |  | Modeling Category, Source, and Site‑Specific scopes in AEM is typically done via a combination of **tags**, CF fields, and site structure. For example, Category and Source as tag namespaces, and a Site‑Specific flag or property indicating which sites should surface an event. The exact mapping for SHRSS to the RFP scopes is an implementation design decision leveraging AEM’s tagging and CF modeling capabilities [Working with Content Fragments - Concepts and Best Practices](https://experienceleague.adobe.com/en/docs/experience-manager-cloud-service/content/sites/administering/content-fragments/overview). |  | Adobe |  | TRUE | 65 |
| Events | How do we design the AEM model to support event harvesting across sites while preserving site-specific content variations and access controls? |  |  | A common approach is to maintain a **central event CF set** (harvested/shared) and have site‑specific overlays through additional fields, related CFs, or site‑specific pages that reference the same events. Access control is then enforced via groups/ACLs on content paths. The concrete design for SHRSS event harvesting and site‑specific variations is implementation‑specific but can be built on this pattern with Content Fragments and Multi‑Site Management. |  | Adobe |  | TRUE | 65 |

:::