# Session Notes — Media — 2026-02-25

**Session:** Media  
**Date:** February 25, 2026  
**Recording:** SHRSS Adobe Knowledge Transfer-20260225_130204  
**Duration:** 1h 34m 37s  
**Primary presenter:** Daniela Tea (Adobe)

---

## Session context

- Follow-up from Locations Day 2: Gonzalo (and Lisa) could not see Google Map regions dropdown—permissions. Generic list “regions” populates that dropdown; user groups did not have read access. Template Authors given read access; Gonzalo confirmed can see now. North America order on prod: republish page after viewing as published to confirm order.
- Media agenda: media gallery, image gallery grid; then review hero banner, hero carousel. Container (layout, sizing) to be separate session.

---

## What was covered

1. **Google Map regions fix** — Dropdown = generic list “regions”; read access via user groups (Template Authors updated). Order on page: view as published, confirm order, then publish. Andy to review Gonzalo’s prod page.
2. **Media gallery** — Images and/or video; select from DAM; alt text, caption, title (caption on image or in lightbox; title in lightbox). Multi-field; reorder. Others: aspect ratio (desktop/tablet/mobile), number of desktop/tablet/mobile tiles, indicator dots, carousel loop. Vertical variation; style variations (caption on image top, primary without indicator). Rick: carousel dots not centered—Daniela noted for fix. Video: Vimeo (or external URL); thumbnail from DAM for tile; same lightbox behavior.
3. **Image gallery grid** — Point to image folder in DAM; select tags (tabs); max number of images (default 50). Tabs = selected tags; only images with that tag in folder show. “All photos” = a tag; to show everything, tag assets with “all photos” too. Append vs replace when bulk-tagging (append = add tag; replace = replace all tags). Title from asset metadata shown below image; no title = “no title.” Don: planning to add titles/descriptions. Daniela: if titles not always desired, capture as enhancement (option to not display title or use description). Edwin: option to uncheck/check title (inherit from asset)? Not for this component—pulls from DAM metadata. Edwin: different titles per property/site for same image? Not currently; control via metadata per asset.
4. **Hero banner / hero carousel** — Image or video (e.g. Vimeo); careers uses video. Options: mute, autoplay, loop, background mode, enable transparency, unmute button, inline playback (iOS). Aspect ratio to avoid black bars. Hero carousel: multiple items; similar config.

---

## Questions, comments, and answers (captured)

*See **Media** sheet in tracker.*

- Regions dropdown: permissions (Template Authors + read on generic list “regions”); Andy to look at prod page.
- North America order: view as published, then publish.
- Media gallery: carousel dots alignment—noted for fix (Rick).
- Image gallery grid: title from asset metadata; no option to toggle title on/off or inherit like alt—enhancement to capture. Different title per property = update metadata per asset; no variation/title override in component.
- Don: adding titles/descriptions to assets.

---

## Product Director — own questions

1. **Governance:** Who can add or edit values in generic lists (e.g. regions)? Who decides which groups get read vs write?
2. **Authoring:** For image gallery grid, one-pager: tag strategy (“all photos” + category tags) and when to use media gallery vs image gallery grid?
3. **DAM:** If we don’t want titles shown in grid, do we capture “hide title” as enhancement or rely on not populating title in metadata?
4. **Gap:** Option to display description instead of (or in addition to) title in image gallery grid; per-asset override for title/description by context (e.g. property).
5. **Permissions:** Which groups need read/write on generic lists used by components (regions, etc.)? Document for KT with Andy.

---

*End of session notes. All Q&A rows in **Media** sheet.*
