## Build and install entire project
```
  cd <your-we.train-project>
  mvn clean install -P autoInstallSinglePackage
```

## Developing with ui.frontend module
```
  cd ui.frontend
  npm run start
  npm run dev
```
